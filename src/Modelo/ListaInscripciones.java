package Modelo;

import java.util.ArrayList;

public class ListaInscripciones extends Lista<Inscripcion> {

    public ListaInscripciones() {
        super();
    }

    /*public ArrayList<Inscripcion> buscarPorSocio(Socio socio) {
        ArrayList<Inscripcion> inscripcionesSocio = new ArrayList<>();
        for (Inscripcion inscripcion : elementos) {
            if (inscripcion.getSocio().equals(socio)) {
                inscripcionesSocio.add(inscripcion);
            }
        }
        return inscripcionesSocio; // Devuelve las inscripciones del socio
    }*/
}
